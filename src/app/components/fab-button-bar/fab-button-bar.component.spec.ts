/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FabButtonBarComponent } from './fab-button-bar.component';

describe('FabButtonBarComponent', () => {
  let component: FabButtonBarComponent;
  let fixture: ComponentFixture<FabButtonBarComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FabButtonBarComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FabButtonBarComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
